<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= product.name %> | Shravii’s Store</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { font-family:'Poppins',sans-serif; background:#f8f9fa; }
    .product-img { max-height:400px; object-fit:cover; border-radius:10px; }
    .btn-order { width:100%; }
  </style>
</head>
<body>
<div class="container mt-5">
  <div class="row">
    <div class="col-md-6 text-center">
      <img src="<%= product.image || '/uploads/placeholder.png' %>" class="img-fluid product-img" alt="<%= product.name %>">
    </div>
    <div class="col-md-6">
      <h2><%= product.name %></h2>
      <p class="text-success fs-5">₹<%= product.price %></p>
      <p class="text-muted"><%= product.description %></p>
      <hr>
      <h5>Place Your Order</h5>
      <form action="/order/submit" method="POST">
        <input type="hidden" name="cartData" value='[{"_id":"<%= product._id %>","name":"<%= product.name %>","price":<%= product.price %>}]'>
        <div class="mb-2">
          <label>Name</label>
          <input type="text" name="name" class="form-control" required>
        </div>
        <div class="mb-2">
          <label>Mobile Number</label>
          <input type="tel" name="mobile" class="form-control" required>
        </div>
        <div class="mb-2">
          <label>Address</label>
          <textarea name="address" class="form-control" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-success btn-order mt-2">Place Order</button>
      </form>
    </div>
  </div>
</div>
</body>
</html>
