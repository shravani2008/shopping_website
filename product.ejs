<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= product.name %> | 🛍️ Shravii’s Store</title>
  <link href="/css/style.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-6 text-center">
      <img src="<%= product.image || '/uploads/placeholder.png' %>" class="img-fluid rounded" style="max-height:400px;">
    </div>
    <div class="col-md-6">
      <h2><%= product.name %></h2>
      <p class="text-success fs-5">₹<%= product.price %></p>
      <p class="text-muted"><%= product.description %></p>

      <h5>Place Your Order</h5>
      <form action="/order/<%= product._id %>" method="POST">
        <div class="mb-2">
          <label>Name</label>
          <input type="text" name="customerName" class="form-control" required>
        </div>
        <div class="mb-2">
          <label>Mobile Number</label>
          <input type="tel" name="mobile" class="form-control" required>
        </div>
        <div class="mb-2">
          <label>Address</label>
          <textarea name="address" class="form-control" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-success mt-2 w-100">Place Order</button>
      </form>
    </div>
  </div>
</div>

</body>
</html>
